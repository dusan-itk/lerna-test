{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.18.3"
  },
  "scripts": {
    "build": "lerna run build",
    "build:watch": "lerna run build:watch:module",
    "bootstrap": "lerna bootstrap --hoist",
    "start": "lerna run start"
  }
}
